# Optional: comma-separated API keys allowed for incoming requests (empty = no validation)
# AGENT_GATEWAY_ALLOWED_API_KEYS=key1,key2

# Portal backend: validate API key and fetch prompt. When set, /agent/run calls this endpoint and uses backend prompt when present.
# AGENT_GATEWAY_PORTAL_BASE_URL=http://host.docker.internal:8000
# AGENT_GATEWAY_PORTAL_VALIDATE_PATH=api/portal/validate/
# AGENT_GATEWAY_PORTAL_VALIDATE_TIMEOUT_SECONDS=10

# LLM system prompt (instructs agent to output JSON array of actions). Multi-line OK in .env.
# AGENT_GATEWAY_LLM_SYSTEM_PROMPT=You are a safety-analysis agent. Given the user prompt and optional website content, output only a JSON array of actions. Use action types: ai_text_detection (field "text"), ai_media_detection (field "media_url"), fact_check (field "facts" array), information_graph (skeleton). No markdown or extra text.

# Featherless AI (openai/gpt-oss-120b)
# AGENT_GATEWAY_LLM_BASE_URL=https://api.featherless.ai/v1
# AGENT_GATEWAY_LLM_API_KEY=your_featherless_api_key
# AGENT_GATEWAY_LLM_MODEL=openai/gpt-oss-120b
# AGENT_GATEWAY_LLM_TIMEOUT_SECONDS=60

# Service endpoints. When agent_gateway runs in Docker (docker-compose), defaults use service names (media_checking:8000 etc). Unset to use those.
# When running agent_gateway on the host (e.g. uvicorn), set to localhost:
# AGENT_GATEWAY_AI_TEXT_DETECTOR_URL=http://localhost:8002
# AGENT_GATEWAY_MEDIA_CHECKING_URL=http://localhost:8000
# AGENT_GATEWAY_FACT_CHECKING_URL=http://localhost:8001
# AGENT_GATEWAY_INFO_GRAPH_URL=
# Content safety (PIL / harmful / unwanted). When running in Docker use http://content_safety:8005; on host use http://localhost:8005
# AGENT_GATEWAY_CONTENT_SAFETY_URL=http://content_safety:8005
# AGENT_GATEWAY_CONTENT_SAFETY_TIMEOUT_SECONDS=120
# AGENT_GATEWAY_SERVICE_TIMEOUT_SECONDS=30

# Media explanation (video/audio/flashcards via Minimax). Docker: http://media_explanation:8006; host: http://localhost:8006
# AGENT_GATEWAY_MEDIA_EXPLANATION_URL=http://media_explanation:8006
# AGENT_GATEWAY_MEDIA_EXPLANATION_TIMEOUT_SECONDS=300
