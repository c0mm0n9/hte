# PostgreSQL (used when running with Docker or a remote Postgres).
# Copy to .env and set POSTGRES_PASSWORD. When using docker compose, these are
# also read by the db container; Django reads them to connect.

POSTGRES_DB=hte
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# When Django runs inside Docker on the same compose network, use:
# POSTGRES_HOST=db
