# Backend agent_gateway (extension /v1/agent/run and /v1/agent/explain are proxied here)
# Local: http://127.0.0.1:8003  |  Docker: http://agent_gateway:8003 or http://host.docker.internal:8003
# GATEWAY_AGENT_GATEWAY_URL=http://127.0.0.1:8003
# GATEWAY_AGENT_GATEWAY_TIMEOUT_SECONDS=300
# GATEWAY_AGENT_GATEWAY_EXPLAIN_TIMEOUT_SECONDS=600

# Optional: fact-check and media-check backends (for /v1/agent/chat and /v1/agent/analyze)
# GATEWAY_FACT_CHECK_BASE_URL=http://localhost:8001
# GATEWAY_MEDIA_CHECK_BASE_URL=http://localhost:8002
