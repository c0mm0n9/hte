# Optional: port on host (default 8080)
# PORT=8080

# Comma-separated list of valid API keys (default: single dummy key)
# DUMMY_VALIDATE_KEYS=dummy-00000000-0000-0000-0000-000000000001-control,dummy-00000000-0000-0000-0000-000000000002-agentic

# Mode returned for valid keys: control | agentic (control includes prompt)
# DUMMY_VALIDATE_MODE=control

# Prompt returned when mode=control
# DUMMY_VALIDATE_PROMPT=You are a helpful assistant.
